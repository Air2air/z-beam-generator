TEST_MODE: true

# AI Detection Configuration for Testing
AI_DETECTION:
  PROVIDER: "mock"
  ENABLED: true
  TARGET_SCORE: 65.0
  MAX_ITERATIONS: 3
  IMPROVEMENT_THRESHOLD: 5.0
  TIMEOUT: 30
  RETRY_ATTEMPTS: 3

# API Configuration for Testing
API:
  PROVIDER: "deepseek"
  TIMEOUT: 30
  RETRY_ATTEMPTS: 2

# Timeouts for different operations (in seconds)
TIMEOUTS:
  API_CALL: 30
  AI_DETECTION: 30
  CONTENT_GENERATION: 120
  OPTIMIZATION: 300

# Component Configuration
COMPONENTS:
  TEXT:
    ENABLED: true
    MAX_TOKENS: 4000
  BULLETS:
    ENABLED: true
    MAX_TOKENS: 2000
  TABLE:
    ENABLED: true
    MAX_TOKENS: 3000

# Logging Configuration
LOGGING:
  LEVEL: "INFO"
  FILE: "test.log"
