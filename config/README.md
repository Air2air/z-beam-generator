# Config Directory

Contains all configuration and settings for the generator system. This includes:
- Application and provider settings
- Section and content configuration
- Environment and directory paths

All configuration should be accessed via the `AppConfig` class for consistency.
