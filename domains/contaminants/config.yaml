# Contaminants Domain Configuration
# Minimal config for coordinator initialization

domain_name: "contaminants"
data_path: "data/contaminants/contaminants.yaml"
data_root_key: "contaminants"
frontmatter_pattern: "{slug}-contamination.yaml"

frontmatter:
  directory: "../z-beam/frontmatter/contaminants"
  filename_pattern: "{slug}-contamination.yaml"

data_adapter:
  data_path: "data/contaminants/contaminants.yaml"
  data_root_key: "contaminants"
  author_key: "author.id"
  context_keys:
    - "category"

# Component types (auto-discovered from prompts/*.txt files)
# Components are dynamically detected at runtime - no hardcoded lists needed
# See: prompts/contaminants/ directory for actual component definitions

# Validation rules (inherits shared defaults)
validation:
  inherit_shared_defaults: true
  # Domain-specific overrides can be added here
