# Domain Frontmatter Field Order Specification
# =============================================================================
# 
# PURPOSE: Centralized template defining standard field order for all domains
# DATE: December 18, 2025
# VERSION: 5.0.0
# 
# STANDARDIZED FIELD ORDER PRINCIPLES:
#   1. IDENTITY & CLASSIFICATION - Top (id, name, category, etc.)
#   2. PAGE METADATA - Core (dates, content_type, schema_version)
#   3. NAVIGATION - Breadcrumb structure
#   4. CHEMICAL IDENTIFIERS - Compounds only (formula, CAS, molecular weight)
#   5. CONTENT FIELDS - AI-generated text (description, micro, faq, etc.)
#   6. IMAGES & MEDIA - Visual assets
#   7. AUTHOR INFORMATION - Complete author object
#   8. EXPOSURE DATA - Compounds only (limits, monitoring)
#   9. MACHINE SETTINGS - Settings domain only
#   10. RELATIONSHIPS - Last section (all cross-references)
# 
# USAGE:
#   - Exporters use this to generate frontmatter in correct order
#   - Validators check frontmatter matches this order
#   - Documentation references this for field requirements
# 
# =============================================================================


# =============================================================================
# BASE FIELDS (Shared by All Domains)
# =============================================================================
# These fields appear at the top of every domain's frontmatter in this order.
# Individual domains may add additional fields to section 1 (Identity).
# =============================================================================

base_fields:
  # ─────────────────────────────────────────────────────
  # 1. IDENTITY & CLASSIFICATION (Base)
  # ─────────────────────────────────────────────────────
  identity_base:
    - id
    - name
  
  # ─────────────────────────────────────────────────────
  # 2. PAGE METADATA (Universal)
  # ─────────────────────────────────────────────────────
  page_metadata:
    - datePublished
    - dateModified
    - content_type
    - schema_version
  
  # ─────────────────────────────────────────────────────
  # 3. NAVIGATION (Universal)
  # ─────────────────────────────────────────────────────
  navigation:
    - full_path
    - breadcrumb
  
  # ─────────────────────────────────────────────────────
  # 4. SEO METADATA (Universal)
  # ─────────────────────────────────────────────────────
  seo_metadata:
    - page_title
    - page_description
    - meta_description
    - seo_description
  
  # ─────────────────────────────────────────────────────
  # 5. CONTENT FIELDS (Commonly Used)
  # ─────────────────────────────────────────────────────
  # Note: 'micro' appears in most domains (not compounds)
  content_common:
    - micro
  
  # ─────────────────────────────────────────────────────
  # 6. IMAGES & MEDIA (Universal)
  # ─────────────────────────────────────────────────────
  media:
    - images
  
  # ─────────────────────────────────────────────────────
  # 7. AUTHOR INFORMATION (Universal)
  # ─────────────────────────────────────────────────────
  author_info:
    - author
  
  # ─────────────────────────────────────────────────────
  # 8. CARD DISPLAY METADATA (Universal)
  # ─────────────────────────────────────────────────────
  card_metadata:
    - card
  
  # ─────────────────────────────────────────────────────
  # 9. RELATIONSHIPS (Universal - Always Last)
  # ─────────────────────────────────────────────────────
  relationships:
    - relationships

# =============================================================================
# MATERIALS DOMAIN
# =============================================================================
materials:
  field_order:
    # ─────────────────────────────────────────────────────
    # 1. IDENTITY & CLASSIFICATION
    # ─────────────────────────────────────────────────────
    - id
    - name
    - category
    
    # ─────────────────────────────────────────────────────
    # 2. PAGE METADATA
    # ─────────────────────────────────────────────────────
    - datePublished
    - dateModified
    - content_type
    - schema_version
    
    # ─────────────────────────────────────────────────────
    # 3. NAVIGATION
    # ─────────────────────────────────────────────────────
    - full_path
    - breadcrumb
    
    # ─────────────────────────────────────────────────────
    # 4. SEO METADATA
    # ─────────────────────────────────────────────────────
    - page_title
    - page_description
    - meta_description
    - seo_description
    
    # ─────────────────────────────────────────────────────
    # 5. CONTENT FIELDS (AI-Generated)
    # ─────────────────────────────────────────────────────
    - micro
    - faq
    
    # ─────────────────────────────────────────────────────
    # 6. IMAGES & MEDIA
    # ─────────────────────────────────────────────────────
    - images
    
    # ─────────────────────────────────────────────────────
    # 7. AUTHOR INFORMATION
    # ─────────────────────────────────────────────────────
    - author
    
    # ─────────────────────────────────────────────────────
    # 8. DOMAIN-SPECIFIC FIELDS
    # ─────────────────────────────────────────────────────
    - subcategory
    - applications
    - characteristics
    - properties
    - components
    - settings_description
    - contamination
    
    # ─────────────────────────────────────────────────────
    # 9. CARD DISPLAY METADATA
    # ─────────────────────────────────────────────────────
    - card
    
    # ─────────────────────────────────────────────────────
    # 10. RELATIONSHIPS (Last Section - includes regulatory_standards)
    # ─────────────────────────────────────────────────────
    - relationships
  
  required_fields:
    - id
    - name
    - category
    - content_type
    - author

# =============================================================================
# CONTAMINANTS DOMAIN
# =============================================================================
contaminants:
  field_order:
    # ─────────────────────────────────────────────────────
    # 1. IDENTITY & CLASSIFICATION
    # ─────────────────────────────────────────────────────
    - id
    - name
    - category
    
    # ─────────────────────────────────────────────────────
    # 2. PAGE METADATA
    # ─────────────────────────────────────────────────────
    - datePublished
    - dateModified
    - content_type
    - schema_version
    
    # ─────────────────────────────────────────────────────
    # 3. NAVIGATION
    # ─────────────────────────────────────────────────────
    - full_path
    - breadcrumb
    
    # ─────────────────────────────────────────────────────
    # 4. SEO METADATA
    # ─────────────────────────────────────────────────────
    - page_title
    - page_description
    - meta_description
    - seo_description
    
    # ─────────────────────────────────────────────────────
    # 5. CONTENT FIELDS
    # ─────────────────────────────────────────────────────
    - micro
    
    # ─────────────────────────────────────────────────────
    # 6. IMAGES & MEDIA
    # ─────────────────────────────────────────────────────
    - images
    
    # ─────────────────────────────────────────────────────
    # 7. AUTHOR
    # ─────────────────────────────────────────────────────
    - author
    
    # ─────────────────────────────────────────────────────
    # 8. DOMAIN-SPECIFIC FIELDS
    # ─────────────────────────────────────────────────────
    - contamination_type
    - removal_difficulty
    - health_hazards
    - detection_methods
    - affected_materials
    
    # ─────────────────────────────────────────────────────
    # 9. CARD DISPLAY METADATA
    # ─────────────────────────────────────────────────────
    - card
    
    # ─────────────────────────────────────────────────────
    # 10. RELATIONSHIPS
    # ─────────────────────────────────────────────────────
    - relationships
  
  required_fields:
    - id
    - name
    - category
    - content_type

# =============================================================================
# COMPOUNDS DOMAIN
# =============================================================================
compounds:
  field_order:
    # ─────────────────────────────────────────────────────
    # 1. IDENTITY & CLASSIFICATION
    # ─────────────────────────────────────────────────────
    - id
    - name
    - display_name
    - category
    - hazard_class
    
    # ─────────────────────────────────────────────────────
    # 2. PAGE METADATA
    # ─────────────────────────────────────────────────────
    - datePublished
    - dateModified
    - content_type
    - schema_version
    
    # ─────────────────────────────────────────────────────
    # 3. NAVIGATION
    # ─────────────────────────────────────────────────────
    - full_path
    - breadcrumb
    
    # ─────────────────────────────────────────────────────
    # 4. SEO METADATA
    # ─────────────────────────────────────────────────────
    - page_title
    - page_description
    - meta_description
    - seo_description
    
    # ─────────────────────────────────────────────────────
    # 4. CHEMICAL IDENTIFIERS
    # ─────────────────────────────────────────────────────
    - chemical_formula
    - cas_number
    - molecular_weight
    
    # ─────────────────────────────────────────────────────
    # 5. CONTENT FIELDS
    # ─────────────────────────────────────────────────────
    - health_effects
    - exposure_guidelines
    - detection_methods
    - first_aid
    
    # ─────────────────────────────────────────────────────
    # 6. IMAGES & MEDIA
    # ─────────────────────────────────────────────────────
    - images
    
    # ─────────────────────────────────────────────────────
    # 7. AUTHOR
    # ─────────────────────────────────────────────────────
    - author
    
    # ─────────────────────────────────────────────────────
    # 8. EXPOSURE DATA
    # ─────────────────────────────────────────────────────
    - exposure_limits
    - health_effects_keywords
    - monitoring_required
    - typical_concentration_range
    - sources_in_laser_cleaning
    
    # ─────────────────────────────────────────────────────
    # 9. CARD DISPLAY METADATA
    # ─────────────────────────────────────────────────────
    - card
    
    # ─────────────────────────────────────────────────────
    # 10. RELATIONSHIPS
    # ─────────────────────────────────────────────────────
    - relationships
  
  required_fields:
    - id
    - name
    - chemical_formula
    - category
    - content_type

# =============================================================================
# SETTINGS DOMAIN
# =============================================================================
settings:
  field_order:
    # ─────────────────────────────────────────────────────
    # 1. IDENTITY
    # ─────────────────────────────────────────────────────
    - id
    - name
    
    # ─────────────────────────────────────────────────────
    # 2. PAGE METADATA
    # ─────────────────────────────────────────────────────
    - datePublished
    - dateModified
    - content_type
    - schema_version
    
    # ─────────────────────────────────────────────────────
    # 3. NAVIGATION
    # ─────────────────────────────────────────────────────
    - full_path
    - breadcrumb
    
    # ─────────────────────────────────────────────────────
    # 4. SEO METADATA
    # ─────────────────────────────────────────────────────
    - page_title
    - page_description
    - meta_description
    - seo_description
    
    # ─────────────────────────────────────────────────────
    # 5. CONTENT
    # ─────────────────────────────────────────────────────
    - micro
    - faq
    
    # ─────────────────────────────────────────────────────
    # 6. IMAGES & MEDIA
    # ─────────────────────────────────────────────────────
    - images
    
    # ─────────────────────────────────────────────────────
    # 7. AUTHOR INFORMATION
    # ─────────────────────────────────────────────────────
    - author
    
    # ─────────────────────────────────────────────────────
    # 8. MACHINE SETTINGS (domain-specific)
    # ─────────────────────────────────────────────────────
    - laser_type
    - power_range
    - pulse_duration
    - wavelength
    - application
    
    # ─────────────────────────────────────────────────────
    # 9. CARD DISPLAY METADATA
    # ─────────────────────────────────────────────────────
    - card
    
    # ─────────────────────────────────────────────────────
    # 10. RELATIONSHIPS (Last Section - includes machine_settings)
    # ─────────────────────────────────────────────────────
    - relationships
  
  required_fields:
    - id
    - name
    - category
    - wavelength
    - relationships
    - author
    - type
    - domain

# =============================================================================
# COMMON NESTED STRUCTURES
# =============================================================================

# Exposure Limits Structure (Compounds)
exposure_limits_structure:
  - osha_pel_ppm
  - osha_pel_mg_m3
  - niosh_rel_ppm
  - niosh_rel_mg_m3
  - acgih_tlv_ppm
  - acgih_tlv_mg_m3

# Domain Linkages Structure (All Domains)
relationships_structure:
  materials:
    - related_contaminants
    - related_compounds
    - related_settings
    - regulatory_standards
  
  contaminants:
    - related_materials
    - produces_compounds
    - recommended_settings
  
  compounds:
    - produced_by_contaminants
    - related_materials
  
  settings:
    - suitable_materials
    - effective_contaminants

# Link Item Structure (All relationships)
link_item_structure:
  - id
  - slug
  - url
  - image
  - frequency  # or other context-specific fields
  - severity
  - typical_context

# Author Structure (All Domains)
author_structure:
  - id
  - name
  - country
  - country_display
  - sex
  - jobTitle
  - expertise
  - affiliation
  - credentials
  - languages
  - email
  - image
  - imageAlt
  - url
  - sameAs
  - persona_file
  - formatting_file

# Affiliation Structure (nested in author)
affiliation_structure:
  - name
  - type

# Breadcrumbs Structure (All Domains)
breadcrumb_item_structure:
  - label
  - url

# =============================================================================
# FIELD TYPE SPECIFICATIONS
# =============================================================================

field_types:
  # String fields
  string_fields:
    - id
    - name
    - slug
    - display_name
    - category
    - description
    - micro
    - type
    - domain
    - chemical_formula
    - cas_number
  
  # Numeric fields
  numeric_fields:
    - density
    - melting_point
    - thermal_conductivity
    - molecular_weight
    - wavelength
    - fluence
    - repetition_rate
  
  # Boolean fields
  boolean_fields:
    - monitoring_required
  
  # List fields
  list_fields:
    - health_effects_keywords
    - sources_in_laser_cleaning
    - expertise
    - credentials
    - languages
    - breadcrumbs
  
  # Object fields
  object_fields:
    - exposure_limits
    - relationships
    - author
    - affiliation
  
  # Nullable fields (can be null)
  nullable_fields:
    - display_name
    - scientific_name
    - description
    - micro
    - faq
    - last_updated

# =============================================================================
# VALIDATION RULES
# =============================================================================

validation:
  # Field naming conventions
  naming_conventions:
    use_snake_case: true
    no_abbreviations: true  # Except standard ones (id, url, etc.)
    descriptive_names: true
  
  # Value conventions
  value_conventions:
    boolean_lowercase: true  # true/false not True/False
    null_lowercase: true     # null not None or NULL
    consistent_units: true   # Document units in field names
  
  # Structure conventions
  structure_conventions:
    alphabetical_lists: false  # Lists maintain semantic order
    nested_object_order: true  # Follow nested structure specs
    consistent_indentation: true  # 2-space YAML indentation
