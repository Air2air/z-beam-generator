name: "JSON-LD Generator"
description: "Generates Schema.org-compliant JSON-LD structured data for laser cleaning articles."
version: "3.0.5"

template: |
  Generate Schema.org structured data content for {article_type} "{subject}" laser cleaning article.

  CONTENT-ONLY FOCUS:
  You provide ONLY the content values. All JSON-LD structure, formatting, and 
  Schema.org compliance will be handled by Python utilities.

  AVAILABLE CONTEXT:
  Subject: {subject}
  Article Type: {article_type}
  Category: {category}
  Chemical Formula: {material_formula}
  Material Symbol: {material_symbol}
  Material Type: {material_type}
  Author: {author_name} ({author_country})

  PRE-FORMATTED ELEMENTS PROVIDED BY PYTHON:
  - Image URLs: {hero_image_url}, {closeup_image_url}
  - Subject slug: {slug}
  - Dates: {created_date}, {updated_date}

  Focus on providing rich, descriptive content for structured data. All JSON formatting, 
  Schema.org compliance, and URL formatting will be handled automatically.
  All JSON formatting, Schema.org compliance, and URL formatting will be handled automatically.
